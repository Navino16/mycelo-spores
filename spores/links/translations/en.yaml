reply:
  list: "services:\n{lines}"
  line: "{label} — {url}"
  noted: "{label} — {url} ({note})"
  empty: "no services are configured yet"
  unknown: "no service called ''{label}''. Try one of: {known}"
  usage: "usage: /link '<label>' — the label of a configured service"
command:
  links:
    description: "List every configured service"
  link:
    description: "Show one service by label"
arg:
  label:
    description: "The label of the service to show"
